import { StyleSheet } from 'react-native';

export const pageDefaultStyles = StyleSheet.create({
  pageWrapper: {
    flex: 1,
    paddingHorizontal: 20,
    paddingTop: 30
  },

  heading: {
    marginBottom: 10,
    marginTop: 40,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 7
  }
});
