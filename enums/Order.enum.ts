export enum ORDER_STATUS {
  pending = '1',
  delivered = '2',
  cancelled = '3',
  deleted = '4',
  modifiedAfterDelivery = '5'
}

export enum HAS_PAID {
  undetermined = '0',
  paid = '1',
  unpaid = '2'
}
